# --- pipeline (as before) ---
langgraph
requests
python-dotenv
gpt-researcher @ https://github.com/assafelovic/gpt-researcher/archive/5cdad9cb434754188b78bd998df18dd8d502cf7e.tar.gz
ddgs  # DuckDuckGo search retriever — gpt-researcher's fallback when TAVILY_API_KEY isn't set
langchain-huggingface  # required by gpt-researcher/LangChain when EMBEDDING points at a HuggingFace embedder
langchain-groq  # gpt-researcher's LLM provider for FAST_LLM/SMART_LLM/STRATEGIC_LLM=groq:... — installing here avoids its broken runtime self-install in this container

sentence-transformers==4.1.0  # GPT Researcher's HuggingFace embedding path

# --- backend API for the React UI ---
fastapi
uvicorn[standard]
python-multipart

# Kept compatible with sentence-transformers and the research pipeline.
transformers==4.51.3
